@model AlertMessage
<div class="row">
    <div class="col-md-12">
        <div class="alert alert-@Model.AlertType">
            <h4 class="alert-title m-0">@Model.Title</h4>
            <p class="m-0">@Model.Message</p>
        </div>

    </div>

</div>

